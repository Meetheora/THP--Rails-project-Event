<div class="container py-5">
  <div class="card shadow border-0">
    <div class="card-body">
      <h2><%= @user.email %></h2>
      <p>Admin : <%= @user.admin? ? "Oui" : "Non" %></p>

      <%= link_to "Modifier", edit_admin_user_path(@user), class: "btn btn-warning" %>
      <%= link_to "Retour", admin_users_path, class: "btn btn-outline-secondary" %>
    </div>
  </div>
</div>